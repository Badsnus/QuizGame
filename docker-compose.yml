version: '3.7'
services:
    web:
        build: .
        command: python quiz/manage.py runserver 0.0.0.0:8000
        volumes:
            - ./quiz/:/usr/src/app/
        ports:
            - 8000:8000
        env_file:
          - quiz/.env
    db:
        image: postgres:15.1
        volumes:
            - postgres_data:/var/lib/postgresql/data/
        env_file:
            - quiz/.env
volumes:
    postgres_data:
